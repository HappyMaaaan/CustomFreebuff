{
  "name": "freebuff-themer",
  "version": "1.0.0",
  "private": true,
  "description": "A small theme studio for Freebuff Desktop — display-only customization, without touching the application files.",
  "type": "module",
  "main": "themer.mjs",
  "bin": {
    "freebuff-themer": "themer.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node themer.mjs",
    "check": "node --check themer.mjs && node --check lib/cdp.mjs && node --check lib/launcher.mjs && echo OK",
    "test": "node test/e2e.mjs && node test/watcher.mjs"
  }
}
